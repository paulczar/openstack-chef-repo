berksfile:
#    options: '--no-freeze --halt-on-frozen'

cookbooks:
# - openstack-common:
# - openstack-block-storage:
# - openstack-compute:
# - openstack-dashboard:
# - openstack-identity:
# - openstack-image:
# - openstack-metering:
# - openstack-network:
# - openstack-object-storage:
# - openstack-ops-database:
# - openstack-ops-messaging:

roles:
- allinone-compute:
- os-base:
- os-block-storage-api:
- os-block-storage-endpoint-registration:
- os-block-storage:
- os-block-storage-scheduler:
- os-block-storage-worker:
- os-compute-api-ec2:
- os-compute-api-metadata:
- os-compute-api-os-compute:
- os-compute-api:
- os-compute-cert:
- os-compute-endpoint-registration:
- os-compute:
- os-compute-scheduler:
- os-compute-single-controller:
- os-compute-vncproxy:
- os-compute-worker:
- os-dashboard:
- os-identity-api-admin:
- os-identity-api:
- os-identity:
- os-image-api:
- os-image-endpoint-registration:
- os-image:
- os-image-registry:
- os-infra-caching:
- os-network-endpoint-registration:
- os-network:
- os-object-storage-account:
- os-object-storage-container:
- os-object-storage-management:
- os-object-storage-object:
- os-object-storage-proxy:
- os-object-storage:
- os-ops-database:
- os-ops-messaging:

environments:
- example:
- vagrant:

data bags:

nodes:
# - ubuntu2-1204.vm:
#     run_list: role[os-ops-messaging],role[os-ops-database],role[os-identity],role[os-image]
#     options: -i ~/.ssh/mray.pem -x mray --sudo -E example
